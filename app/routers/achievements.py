"""Achievements router (stub — endpoints added during the achievements module build)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/achievements", tags=["Achievements"])
