"""Roadmaps router (stub — endpoints added during the roadmaps module build)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/roadmaps", tags=["Roadmaps"])
