"""Settings router (stub — endpoints added during the settings module build)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/settings", tags=["Settings"])
