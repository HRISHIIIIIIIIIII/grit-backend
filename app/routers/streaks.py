"""Streaks router (stub — endpoints added during the streaks module build)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/streaks", tags=["Streaks"])
