"""Notifications router (stub — endpoints added during the notifications module build)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/notifications", tags=["Notifications"])
